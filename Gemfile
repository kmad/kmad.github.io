# frozen_string_literal: true

source "https://rubygems.org"

# gem "rails"
gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"  # Required for Ruby >= 3.0
gem "public_suffix", "~> 5.1.1"  # Pin to the version required by github-pages
gem "faraday-retry"  # Add retry middleware for Faraday